

// Exemple de service pour récupérer un utilisateur par ID
function getUserById(id: number) {
  // Ici vous pourriez interroger votre base de données
  // Dans cet exemple, on renvoie un utilisateur fictif
}

module.exports = {
  getUserById,
};
